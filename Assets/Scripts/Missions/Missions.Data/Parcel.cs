namespace Missions.Missions.Data
{
    public enum EParcelType
    {
        Standard,
        Lightweight, // Minor speed boost
        Fragile, // Fails on any collision
        Heavy // Imposes a speed limit
    }
}